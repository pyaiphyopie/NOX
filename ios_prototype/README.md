# NOX Prototype iOS App

Native SwiftUI prototype for the NOX mobile experience.

## Build on macOS

1. Open `NOXPrototype.xcodeproj` in Xcode.
2. Select the `NOXPrototype` scheme.
3. Pick an iPhone simulator.
4. Press Run.

The project is dependency-free and uses SwiftUI only.
