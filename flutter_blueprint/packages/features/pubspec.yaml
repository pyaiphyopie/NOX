name: nox_flutter_features
description: Feature modules for the NOX Flutter monorepo.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  go_router: ^17.2.3
  nox_flutter_core:
    path: ../core
  nox_flutter_shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
