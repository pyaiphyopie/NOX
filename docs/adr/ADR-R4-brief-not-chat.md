# ADR-R4: AI is a stored Night Brief, not a chat product

**Status:** Proposed  
**Date:** 2026-09-10

Generate at T-3h and at doors-close. Persist `input_snapshot_json` + `body_text`. MVP is deterministic template. LLM rewrite is an adapter behind an interface and is off until Jake enables it.
